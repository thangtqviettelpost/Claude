[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "claude"
version = "0.1.0"
requires-python = ">=3.8"

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I"]
